{
  "name": "flowgraph",
  "version": "1.0.0",
  "description": "FlowGraph — Interactive dependency flowchart viewer with Design Mode, Focus Mode, and SharePoint adapter support.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:standalone": "tsc && vite build --config vite.standalone.config.ts",
    "preview": "vite preview",
    "lint": "eslint src --ext ts,tsx"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.6",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
